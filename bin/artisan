#!/usr/bin/env bash
USER_ID="$UID"

# $USER doesn't have acces to octane process
# anyway it doesn't work with frankephp, and you should restart container
docker compose exec -u $USER_ID \
  backend \
  php /app/artisan "$@"
