# core api inside container (used in server component)
CORE_API_ENTRYPOINT=<ChangeMe>
# core api outside container (used in client component)
NEXT_PUBLIC_CORE_API_ENTRYPOINT=<ChangeMe>:8000
# real URL
NEXT_PUBLIC_SITE_URL=<ChangeMe>
