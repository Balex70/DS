# core api inside container (used in server component)
# should be name of server from kube not name of pod (some-backend-service:8000)
CORE_API_ENTRYPOINT=<ChangeMe>
# core api outside container (used in client component: https://domain.local)
NEXT_PUBLIC_CORE_API_ENTRYPOINT=https://<ChangeMe>
